//  WChat
//
//  Created by Mohamed Adel on 7/4/20.
//  Copyright © 2020 Mohamed Adel. All rights reserved.
//


import Foundation
/*
class UserTokenManager: NSObject
{
    private override init() { super.init() }
    
    static var userToken: String?
    {
       // let key = UserDefaultsKey.User.token.rawValue
        let token = UserDefaults.standard.string(forKey: key)
        return token
    }
    
    static var deviceToken: String?
    {
        let key = UserDefaultsKey.User.deviceToken.rawValue
        let token = UserDefaults.standard.string(forKey: key)
        return token
    }
    
    static func addDeviceToken(_ token: String)
    {
        let key = UserDefaultsKey.User.deviceToken.rawValue
        UserDefaults.standard.set(token, forKey: key)
        UserDefaults.standard.synchronize()
    }
    
    static func addUserToken(_ token: String)
    {
        let key = UserDefaultsKey.User.token.rawValue
        UserDefaults.standard.set(token, forKey: key)
        UserDefaults.standard.synchronize()
    }
    
    static func resetUserSession()
    {
        let tokenKey = UserDefaultsKey.User.token.rawValue
        let deviceKey = UserDefaultsKey.User.deviceToken.rawValue
        let userkey = UserDefaultsKey.User.currentUser.rawValue
        UserDefaults.standard.removeObject(forKey: userkey)
        UserDefaults.standard.removeObject(forKey: tokenKey)
        UserDefaults.standard.removeObject(forKey: deviceKey)
        UserDefaults.standard.synchronize()
    }
}
*/
